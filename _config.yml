title: GHTNS
description: Memorializing personal experiences and thoughts 
github_username:  jalangle

# Build settings
markdown: kramdown
theme: minima

collections:
  photosites:
    output: true

defaults:
  -
    scope:
      path: ""
      type: "photosites"
    values:
      layout: "photosite"

plugins:
  - jekyll-feed

exclude:
  - bin
  - venv